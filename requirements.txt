# Professional Layout Review Tool - Requirements
# Python 3.8+

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
plotly>=5.0.0
kaleido>=0.2.0  # For plotly image export

# Geometric operations (shapely for polygon boolean ops, distance calculations)
shapely>=2.0.0

# Dash UI (using local Bootstrap CSS from assets/ folder)
# Pin <3: Dash 4.x requires Jupyter comm and breaks CLI startup on Python 3.8
dash>=2.0.0,<3.0.0

# PPTX generation
python-pptx>=0.6.21

# PDF generation (optional)
reportlab>=3.6.0

# Data processing
openpyxl>=3.0.0  # For Excel export if needed

# YAML support for batch import
pyyaml>=6.0
